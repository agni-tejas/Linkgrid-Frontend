/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "utfs.io",
      "assets.aceternity.com",
      "subdomain",
      "images.unsplash.com",
      "pbs.twimg.com",
    ],
  },
  reactStrictMode: false,
};

export default nextConfig;
